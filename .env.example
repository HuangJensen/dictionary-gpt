# ===== MySQL 连接配置 =====
# 本地 MySQL 或 TiDB Cloud Serverless（免费、MySQL 兼容）都可以
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=dictionary

# 使用 TiDB Cloud Serverless 时必须设为 true（要求 TLS）
DB_SSL=false

# 连接池大小：越小越省内存（默认 5）
DB_POOL_SIZE=5

# 服务端口
PORT=3000
